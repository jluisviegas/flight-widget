FLIGHT WIDGET:

Hard Coded data with flights information

DOM Manipulation:
	Grabbing the element on HTML
	Adding element dynamically with JS
	Appending elements to the DOM

For loops:
	for...of statement
	for...in statement
	Iterating with index and element

Array methods:
	Array.prototype.shift()
	Array.prototype.entries()
	Array.prototype.push()
	Array.prototype.slice()



setTimeout()
Math.floor()
Math.random()
String.prototype.charAt()
